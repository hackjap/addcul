<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/illuminating_yellow</item>
        <item name="colorPrimaryDark">@color/ultimate_gray</item>
        <item name="colorAccent">@color/basic_text</item>
        <item name="android:fontFamily">@font/app_main_font</item>
    </style>

</resources>
